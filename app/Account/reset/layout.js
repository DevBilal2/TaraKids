export const metadata = {
  title: "Set new password",
  description: "Choose a new password for your Tara Kids account.",
  robots: { index: false, follow: false },
};

export default function AccountResetLayout({ children }) {
  return children;
}
